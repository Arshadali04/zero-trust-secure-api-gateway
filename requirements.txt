# ============================================================================
# ZERO TRUST SECURE API GATEWAY - PRODUCTION REQUIREMENTS
# Python: 3.10+
# ============================================================================

# --- WEB FRAMEWORK & SERVER ---
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0
pydantic-settings==2.3.0
python-multipart==0.0.9
httptools==0.6.2

# --- DATABASE & ORM ---
SQLAlchemy==2.0.36
aiosqlite==0.20.0
alembic==1.14.0

# --- AUTHENTICATION & SECURITY ---
PyJWT==2.10.0
cryptography==43.0.0
passlib==1.7.4           # argon2 backend used (not bcrypt)
argon2-cffi==23.1.0
pyotp==2.9.0
qrcode[pil]==7.4.2
Authlib==1.4.0
certifi==2024.12.14

# --- CACHING & RATE LIMITING ---
redis==5.2.0

# --- MACHINE LEARNING (Anomaly Detection) ---
scikit-learn==1.5.2
numpy==1.26.4
joblib==1.4.2
# scipy removed — was listed but never used in codebase

# --- OBSERVABILITY ---
python-json-logger==2.0.7
prometheus-client==0.21.0
opentelemetry-api==1.27.0
opentelemetry-sdk==1.27.0
opentelemetry-exporter-prometheus==0.48b0
opentelemetry-instrumentation-fastapi==0.48b0

# --- HTTP CLIENT (Proxy Engine) ---
httpx==0.27.0

# --- UTILITIES ---
python-dotenv==1.0.1
email-validator==2.2.0
orjson==3.10.7
PyYAML==6.0.2

# --- OPTIONAL: ACCURATE IMPOSSIBLE-TRAVEL DETECTION ---
# Not installed by default — it needs a MaxMind account and a ~60MB database, so
# it is opt-in rather than a hard dependency. Without it,
# gateway/detection/context_validation.py runs in degraded mode and logs a
# warning saying so.
#
#   pip install geoip2==4.8.0
#   # then download GeoLite2-City.mmdb into data/geoip/  (see data/geoip/.gitkeep)
#
# geoip2==4.8.0
